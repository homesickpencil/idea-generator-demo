# Copy to .env for local dev, or set in the Vercel project's Environment Variables.
# Vite only exposes vars prefixed with VITE_ to the browser.

VITE_BACKEND_URL=http://localhost:8000   # the deployed backend URL in production
